# Logs
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Audit + build artifacts (regenerated, not source of truth)
test-results/
playwright-report/
blob-report/
playwright/.cache/
*.tsbuildinfo

# A separate clone of the team backend repo (AlvinEmenda/Emenda_tech) lives
# here; it has its own .git and must never be committed into this one.
"emenda front-end"/
